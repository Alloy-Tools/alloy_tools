[package]
name = "al-pipe"
version.workspace = true
license.workspace = true
repository.workspace = true
edition.workspace = true

[lib]
doctest = false

[dependencies]
al-core = { path = "../al-core", features = ["binary"] }
tokio = { version = "1", features = ["sync", "net", "time", "io-util"] }
